# Sample images

Drop reference / inspiration images for the v2 redesign here (PNG, JPG, SVG,
WebP). Anything in this folder is input for design work only — nothing here is
bundled into the app or served at runtime.

Sibling folders:

- `../before/` — screenshots of the current (v1) site, used as the baseline.
- `../Haohaotiantian Redesign.dc.html` — the redesign mockup.

Assets that the app actually ships live in `v2/src/assets/` instead.
